// Contractory — Polygon Adapter (Stub)
import type { BlockchainAdapter } from '../../core/interface'
export const polygonAdapter: Partial<BlockchainAdapter> = {
  id: 'polygon', name: 'Polygon', chainId: 137, testnet: false,
}
